[project]
name = "vw-gw"
version = "0.1.0"
description = "Videowall media gateway (ingest/probe)"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.115.6",
  "uvicorn[standard]==0.30.6",
  "pydantic==2.9.2",
]

[tool.uvicorn]
factory = false
