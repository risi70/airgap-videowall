{{- if .Values.namespaces.obs.defaultDenyEnabled | default true }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: {{ .Values.namespaces.obs.name | default "vw-observability" }}
  labels:
    app.kubernetes.io/part-of: videowall
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
{{- end }}
