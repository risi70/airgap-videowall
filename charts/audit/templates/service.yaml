apiVersion: v1
kind: Service
metadata:
  name: vw-audit
  labels:
    app: vw-audit
spec:
  type: {{ .Values.service.type }}
  selector:
    app: vw-audit
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
