apiVersion: v1
kind: Service
metadata:
  name: vw-health
  labels:
    app: vw-health
spec:
  type: {{ .Values.service.type }}
  selector:
    app: vw-health
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
