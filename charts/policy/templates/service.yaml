apiVersion: v1
kind: Service
metadata:
  name: vw-policy
  labels:
    app: vw-policy
spec:
  type: {{ .Values.service.type }}
  selector:
    app: vw-policy
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
