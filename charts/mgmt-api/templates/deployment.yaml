apiVersion: apps/v1
kind: Deployment
metadata:
  name: vw-mgmt-api
  labels:
    app: vw-mgmt-api
spec:
  replicas: { { .Values.replicaCount } }
  selector:
    matchLabels:
      app: vw-mgmt-api
  template:
    metadata:
      labels:
        app: vw-mgmt-api
      annotations:
        {{- if .Values.vault.enabled }}
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/agent-inject-secret-tls.crt: "{{ .Values.vault.pkiPath }}"
        vault.hashicorp.com/role: "{{ .Values.vault.role | default "mgmt-api" }}"
        {{- end }}
    spec:
      securityContext:
{ toYaml .Values.podSecurityContext | indent 8 }
      containers:
        - name: vw-mgmt-api
          image: "{{ .Values.global.registry }}/{{ .Values.image.repository }}:{{ .Values.global.imageTag }}"
          imagePullPolicy: "{{ .Values.global.imagePullPolicy }}"
          ports:
            - containerPort: { { .Values.service.port } }
          env:
{- range $k, $v := .Values.env }
            - name: { $k }
              value: "{ $v }"
{- end }
          securityContext:
{ toYaml .Values.securityContext | indent 12 }
          resources:
{ toYaml .Values.resources | indent 12 }
