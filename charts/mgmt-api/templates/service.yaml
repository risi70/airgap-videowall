apiVersion: v1
kind: Service
metadata:
  name: vw-mgmt-api
  labels:
    app: vw-mgmt-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: vw-mgmt-api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
